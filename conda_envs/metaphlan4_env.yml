# MetaPhlAn 4.1.1: the release that matches the mpa_vJun23_CHOCOPhlAnSGB_202307 database
# (--metaphlan_index). 4.0.6 + vJun23 was an unsupported pairing (audit D06; Ammal 2026-09-23).
# The exact build is conda_envs/lock/metaphlan4_env.linux-64.txt; this file is its source.
name: metaphlan4_env
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - python=3.11
  - metaphlan=4.1.1
  - bowtie2=2.5.4
  - samtools=1.21
