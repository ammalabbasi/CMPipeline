/* Generic Slurm profile. Supply site-specific queue/account in an overlay config. */

executor {
    name = 'slurm'
    queueSize = 100
    submitRateLimit = '1 sec'
}

process {
    executor = 'slurm'
}
