/* Generic Sun/Oracle Grid Engine execution profile. Add the site queue/project with -c. */

executor {
    name = 'sge'
    queueSize = 100
    submitRateLimit = '1 sec'
}

process {
    executor = 'sge'
}
